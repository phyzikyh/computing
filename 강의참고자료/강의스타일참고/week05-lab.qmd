# 5주차 코딩 실습 {#sec-ch5-lab}

이 실습은 작은 CNN을 직접 만들어 이미지를 분류하고, 사전학습 모델을 이용한 전이학습을 경험합니다. **예제 코드**를 먼저 실행해 동작을 확인한 뒤, **제출용 문제**의 빈칸(`# TODO`)을 채워 제출합니다.

## 예제 코드 (함께 실행)

FashionMNIST(28×28 흑백, 의류 10종)로 CNN 분류기를 학습합니다. 데이터는 `torchvision`이 자동으로 내려받습니다.

```python
import torch, torch.nn as nn, torch.optim as optim
import torchvision
from torchvision import transforms
from torch.utils.data import DataLoader

device = "cuda" if torch.cuda.is_available() else "cpu"
torch.manual_seed(0)

# 1) 데이터: FashionMNIST (자동 다운로드)
tf = transforms.Compose([
    transforms.ToTensor(),                     # (1, 28, 28), 0~1
    transforms.Normalize((0.286,), (0.353,)),  # 채널 표준화
])
train_ds = torchvision.datasets.FashionMNIST(root="./data", train=True,  download=True, transform=tf)
test_ds  = torchvision.datasets.FashionMNIST(root="./data", train=False, download=True, transform=tf)
train_loader = DataLoader(train_ds, batch_size=128, shuffle=True,  num_workers=2)
test_loader  = DataLoader(test_ds,  batch_size=256, shuffle=False, num_workers=2)

# 2) 작은 CNN 정의: Conv-ReLU-Pool 두 묶음 → FC
class SmallCNN(nn.Module):
    def __init__(self, n_classes=10):
        super().__init__()
        self.features = nn.Sequential(
            nn.Conv2d(1, 32, kernel_size=3, padding=1),  # (32, 28, 28)
            nn.ReLU(),
            nn.MaxPool2d(2),                             # (32, 14, 14)
            nn.Conv2d(32, 64, kernel_size=3, padding=1), # (64, 14, 14)
            nn.ReLU(),
            nn.MaxPool2d(2),                             # (64, 7, 7)
        )
        self.classifier = nn.Sequential(
            nn.Flatten(),
            nn.Linear(64*7*7, 128), nn.ReLU(), nn.Dropout(0.3),
            nn.Linear(128, n_classes),
        )
    def forward(self, x):
        return self.classifier(self.features(x))

model = SmallCNN().to(device)
crit = nn.CrossEntropyLoss()
opt  = optim.Adam(model.parameters(), lr=1e-3)

def run(loader, train):
    model.train() if train else model.eval()
    L = C = N = 0
    with torch.set_grad_enabled(train):
        for xb, yb in loader:
            xb, yb = xb.to(device), yb.to(device)
            out = model(xb); loss = crit(out, yb)
            if train:
                opt.zero_grad(); loss.backward(); opt.step()
            L += loss.item()*len(xb); C += (out.argmax(1)==yb).sum().item(); N += len(xb)
    return L/N, C/N

for ep in range(1, 6):
    tr = run(train_loader, True)
    te = run(test_loader, False)
    print(f"[{ep}] train_acc={tr[1]:.3f}  test_acc={te[1]:.3f}")
```

## 제출용 문제

**문제 1. 커널·채널 실험.** `SmallCNN`의 필터 수(32, 64)를 (16, 32)와 (64, 128)로 바꿔 각각 테스트 정확도와 파라미터 수를 비교하세요. 파라미터 수는 아래로 셀 수 있습니다.

```python
def count_params(model):
    # TODO: model.parameters() 중 requires_grad=True인 것의 원소 수 합을 반환
    ...
```

**문제 2. 배치 정규화 효과.** 각 `Conv2d` 뒤(ReLU 앞)에 `nn.BatchNorm2d`를 추가한 모델을 만들어, 배치 정규화가 있을 때와 없을 때의 학습 속도(에폭별 정확도)와 최종 정확도를 비교하세요.

```python
# 힌트: nn.Conv2d(...) 다음에 nn.BatchNorm2d(out_channels)를 넣는다
```

**문제 3. 데이터 증강.** 학습 변환에 무작위 좌우 뒤집기와 무작위 자르기를 추가해, 증강이 테스트 정확도와 과적합(훈련-테스트 정확도 격차)에 미치는 영향을 관찰하세요.

```python
train_tf = transforms.Compose([
    # TODO: transforms.RandomHorizontalFlip(),
    #       transforms.RandomCrop(28, padding=4) 를 추가
    transforms.ToTensor(),
    transforms.Normalize((0.286,), (0.353,)),
])
```

**문제 4. 전이학습.** 사전학습된 ResNet-18을 이용해 전이학습을 수행하세요. 아래 골격을 완성하되, (a) **특징 추출**(합성곱부 동결)과 (b) **미세조정**(전체 학습, 낮은 학습률) 두 전략의 정확도를 비교합니다. 흑백 FashionMNIST를 3채널로 확장하고 크기를 키워 입력합니다.

```python
import torchvision.models as models

# 입력 변환: 1채널→3채널 확장, 크기 확대, ImageNet 통계로 정규화
tf3 = transforms.Compose([
    transforms.Grayscale(num_output_channels=3),
    transforms.Resize(224),
    transforms.ToTensor(),
    transforms.Normalize([0.485,0.456,0.406], [0.229,0.224,0.225]),
])

def build_resnet(strategy: str, n_classes=10):
    net = models.resnet18(weights=models.ResNet18_Weights.DEFAULT)
    if strategy == "feature":
        # TODO: 모든 파라미터의 requires_grad를 False로 (합성곱부 동결)
        ...
    # TODO: net.fc를 nn.Linear(net.fc.in_features, n_classes)로 교체
    return net.to(device)

# (a) strategy="feature"와 (b) strategy="finetune"으로 각각 몇 에폭 학습해 정확도 비교
# 미세조정은 낮은 학습률(예: 1e-4), 특징 추출은 분류기만 1e-3로 학습
```

**제출물**: 완성된 `.py` 파일과 문제 1~4의 결과 표·그래프, 그리고 전이학습 두 전략의 정확도·학습시간 비교와 3~5문장 분석.
